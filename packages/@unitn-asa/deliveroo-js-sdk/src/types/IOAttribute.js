/**
 * A numeric attribute exposed by a plugin on an agent or entity.
 * @typedef IOAttribute
 * @property {string} kind
 * @property {number} value
 * @property {number=} max - Maximum value, when bounded
 */

export { };
