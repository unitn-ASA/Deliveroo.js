// @ts-check

/**
 * @typedef {import('@unitn-asa/types').IOAgent & {
 *   mesh?: import('three').Mesh;
 *   hoovered?: boolean;
 *   selected?: import('vue').ComputedRef<boolean>;
 *   status?: 'online' | 'offline' | 'out of range';
 *   opacity?: number;
 *   carrying?: string[];
 * }} UIAgent
 */

export const UIAgent = /** @type {UIAgent} */ ({});