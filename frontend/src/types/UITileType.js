// @ts-check

/**
 * @typedef {import('@unitn-asa/deliveroo-js-sdk').IOTile & {
 *   mesh?: import('three').Mesh;
 *   hoovered?: boolean;
 *   selected?: boolean;
 *   perceivingAgents?: boolean;
 *   perceivingParcels?: boolean;
 *   opacity?: number;
 * }} UITile
 */

export const UITile = /** @type {UITile} */ ({});