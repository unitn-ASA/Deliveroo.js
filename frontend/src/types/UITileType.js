// @ts-check

/**
 * @typedef {import('@unitn-asa/types').IOTile & {
 *   mesh?: import('three').Mesh;
 *   hoovered?: boolean;
 *   selected?: boolean;
 *   perceivingAgents?: import('vue').ComputedRef<boolean>;
 *   perceivingParcels?: import('vue').ComputedRef<boolean>;
 *   opacity?: number;
 * }} UITile
 */

export const UITile = /** @type {UITile} */ ({});