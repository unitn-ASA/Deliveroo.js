// @ts-check

/**
 * @typedef {{
 *   id: string,
 *   x: number,
 *   y: number,
 *   mesh?: import('three').Mesh,
 *   hoovered?: boolean,
 *   selected?: boolean
 * }} UICrate
 */

export const UICrate = /** @type {UICrate} */ ({});
